{
  "name": "desgin-system",
  "version": "0.0.0",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "scripts": {
    "build": "nx build",
    "graph": "nx run-many --target=build --graph"
  },
  "packageManager": "yarn@4.1.0",
  "devDependencies": {
    "nx": "^18.0.5"
  }
}
